import styles from "./Contact.module.css";
import React from "react";

function template() {
  return (
    <div className={`contact ${styles.clr}`}>
      <h1>Contact</h1>
    </div>
  );
};

export default template;
